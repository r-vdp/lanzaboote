[workspace]
resolver = "2"

members = [
    "bootspec",
    "shared",
    "systemd",
]

default-members = [
    "bootspec",
    "shared",
    "systemd"
]

[workspace.package]
version = "0.4.2"
edition = "2021"

[profile.release]
opt-level = "s"
lto = true
